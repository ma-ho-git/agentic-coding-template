# Beispiele

Dieser Ordner demonstriert den Arbeitszyklus des Templates an einem winzigen, echten
Feature (`slugify/`) — TDD, Contract-Kommentare über zwei Dateien, `/contract-sync` bei
einer Vertragsänderung. Siehe `slugify/README.md` für den Ablauf.

<!-- template-placeholder -->
**Beim Start eines eigenen Projekts: diesen Ordner löschen.** Er ist Dokumentation für das
Template selbst, kein Teil des Projektgerüsts.
