---
title: Fortschritt 2026-08
aliases: ["Fortschritt 2026-08"]
type: progress
tags: [topic/meta]
status: active
created: 2026-08-19
updated: 2026-08-19
review_after: 2027-08-19
related: ["[[00-index]]"]
---

# Fortschritt 2026-08

## 2026-08-19 — Projektstart, Analyse und Recherche

- Ergebnis: Vorgaben analysiert, aktuelle Fakten zu Claude Code, Cowork und obsidian-kanban
  recherchiert und im Vault abgelegt
- Zentrale Erkenntnisse:
  - `CLAUDE.md` ist keine Durchsetzungsebene → prüfbare Vorgaben gehören in Hooks
  - Cowork liest die Repo-Konfiguration nicht → eigener Verteilungsweg nötig
  - Board-Datei als einziger Schreibpunkt kollidiert mit Parallelarbeit
- Entscheidungen: [[ADR-0001 Regeln aufteilen statt eine große CLAUDE.md]],
  [[ADR-0002 Prüfbare Vorgaben in Hooks statt in Prosa]],
  [[ADR-0003 Kanban-Board mit Wikilink-Karten]],
  [[ADR-0004 Cowork über ein leichtes Paket anbinden]]
- Neu gelernt: [[Konfigurationsebenen von Claude Code]],
  [[Cowork liest die Repo-Konfiguration nicht]], [[Obsidian-Kanban Dateiformat]],
  [[Dokumentationsstandards je Sprache]]

## 2026-08-19 — T-0001 Agentenregeln und Repo-Grundgerüst

- Ergebnis: `CLAUDE.md` (94 Zeilen) plus sieben Regeldateien; vier davon pfad-gebunden
- Offene Folgeaufgaben: keine

## 2026-08-19 — T-0002 Hooks zur Durchsetzung der Vorgaben

- Ergebnis: fünf Hook-Skripte, gegen Positiv- und Negativfälle getestet
- Aufteilung: Geheimnisse, fehlender Contract-Header und gefährliche Kommandos blockieren;
  Größen- und Namensgrenzen sowie veraltetes Contract-Datum erzeugen Hinweise
- Offene Folgeaufgaben: [[T-0010 CI-Workflow für die Regelprüfung]]

## 2026-08-19 — T-0003 Wissensdatenbank aufsetzen (laufend)

- Ergebnis: Vault-Struktur, Konventionen, vier Templates, Board, zehn Tasks, vier ADRs,
  vier Wissensnotizen, eine Troubleshooting-Notiz
- Der Vault wird bereits für dieses Projekt selbst geführt, nicht nur vorbereitet
- Offen: Vollständigkeitsprüfung der Verlinkung ([[T-0007 Selbstverifikation des Templates]])

## 2026-08-19 — Blockade beim Repo-Zugriff

- Push scheitert am Git-Proxy: [[Git-Proxy verweigert Push - Repo nicht in der Session freigegeben]]
- Zuständig: Mensch, siehe [[T-0008 Schreibzugriff auf das GitHub-Repo]]

## 2026-08-19 — T-0004 bis T-0006 abgeschlossen

- Ergebnis: Stack-Profile (Vorlage, Python, TypeScript), Cowork-Onboarding-Paket
  (Instructions, README, zwei `.skill`-Pakete, Build-Skript), deutschsprachige README, MIT-Lizenz
- Offene Folgeaufgaben: keine

## 2026-08-19 — T-0007 Selbstverifikation (laufend)

- `tools/check_vault.py` geschrieben: Frontmatter-Pflichtfelder, Wikilink-Auflösung,
  Waisen, Erreichbarkeit, `CLAUDE.md`-Länge. Vault meldet 0 Fehler, 0 Warnungen.
- Beim Prüflauf gefunden und behoben:
  - Dateiname und Wikilink-Ziel liefen bei den `90-meta`-Notizen auseinander
    → `aliases`-Feld eingeführt und in den Konventionen dokumentiert
  - Beispiel-Wikilinks in Codeblöcken wurden als echte Links gewertet
    → Prüfskript ignoriert jetzt Code-Fences und Inline-Code
  - `board.md` hat ein eigenes Frontmatter-Schema
    → wird nur noch als Linkquelle geprüft, nicht auf Pflichtfelder
- Eigene Hooks gegen das eigene Werkzeug laufen lassen: `check_quality.py` monierte
  fünf Parameter in `check_links()` → refaktoriert auf drei. Die Regel greift also real.
- Offen: Rendering des Boards in Obsidian (nur der Mensch kann das bestätigen),
  Trockenlauf von `/bootstrap` in einem frischen Klon

## 2026-08-19 — `/bootstrap` ausgeführt (Umsetzungspfad Schritt 1)

- Anlass: SessionStart-Hook meldete "BOOTSTRAP REQUIRED", weil der Manifest-Eintrag
  "Aktiver Stack" unverifiziert war (`stacks/active.md` fehlte)
- Alle übrigen Manifest-Einträge waren bereits heute verifiziert → nicht erneut geprüft
- Geprüft: `.claude/hooks`- und `tools/`-Dateien, installierte Toolchain (`uv`, `pytest`,
  `ruff`, `mypy` vorhanden), tatsächliche Python-Version (3.11.15, nicht 3.12+ wie in
  `stacks/python.md` behauptet)
- Ergebnis: `stacks/active.md` angelegt — Python-Profil, aber Layout auf `.claude/hooks/`
  + `tools/` statt `src/<package>/` umgestellt (dieses Repo hat kein Anwendungspaket, nur
  Tooling), Python-Version korrigiert, fehlendes `pyproject.toml` für Tests an
  [[T-0007 Selbstverifikation des Templates]] verwiesen statt hier nebenbei angelegt
- `knowledge/00-index.md` und `README.md` enthielten keine Platzhalter mehr (Projekt ist
  das Template selbst) → Schritt "Projektname/Zweck erfragen" entfiel
- Board hatte bereits alle nötigen Tasks (T-0001–T-0010) → keine neuen Tasks geseedet

## 2026-08-19 — T-0008 Schreibzugriff auf das GitHub-Repo (Umsetzungspfad Schritt 2)

- Ergebnis: `git push -u origin claude/vibe-coding-template-kb-03odfw` gelang direkt aus
  dieser Session — beide Akzeptanzkriterien erfüllt, Lösungsweg war bereits vollzogen
  (Repo als Source zugeordnet, Push-Branch fest zugewiesen)
- Entscheidungen: keine neuen
- Neu gelernt: nichts, was eine eigene Notiz rechtfertigt — die bestehende
  [[Git-Proxy verweigert Push - Repo nicht in der Session freigegeben]] deckt den Fall
  bereits ab
- Offene Folgeaufgaben: keine

## 2026-08-19 — T-0007 Selbstverifikation abgeschlossen (Umsetzungspfad Schritt 3)

- Ergebnis: `tests/hooks/` mit 17 Tests (Positiv+Negativ) gegen alle fünf Hook-Skripte,
  alle grün; `ruff check` sauber. Karte nach Review verschoben — nur noch das
  Obsidian-Rendering braucht den Menschen.
- Echter Bug gefunden und behoben: `session_brief.py` meldete wegen einer zu engen Regex
  IMMER "BOOTSTRAP REQUIRED", auch bei aktuellem Manifest — mit frischem `git clone` gegen
  den gepushten Stand real reproduziert, nicht nur lokal vermutet.
- `config.json`-Kommentar korrigiert (verwies auf nicht existierendes `check_file.py`).
- Neu gelernt: [[SessionStart meldet BOOTSTRAP REQUIRED trotz aktuellem Manifest]] —
  Hook-Tests müssen das reale Dateiformat nutzen, nicht ein vereinfachtes Fixture, sonst
  fangen sie genau solche Bugs nicht.
- Offene Folgeaufgaben: keine (Obsidian-Rendering bleibt manueller Schritt für den Nutzer)

## 2026-08-19 — T-0010 CI-Workflow abgeschlossen (Umsetzungspfad Schritt 4)

- Ergebnis: `.github/workflows/rules.yml` + `tools/ci_check.py` (20/20 Tests grün,
  `ruff` sauber). Wiederverwendet die bestehenden Hooks statt einer zweiten
  Regel-Implementierung; läuft zusätzlich `check_vault.py` und die volle Test-Suite.
- Dabei einen zweiten, unabhängigen Bug gefunden: `_common.py#matches_any()` matcht
  Glob-Muster über Pfadgrenzen hinweg (fnmatch ohne `/`-Bewusstsein) — nicht sofort
  behoben, eigener Task angelegt.
- Entscheidungen: keine neuen ADR nötig — Ansatz folgt direkt aus ADR-0002
- Neu gelernt: [[matches_any() exemption trifft falsche Pfade]]
- Offene Folgeaufgaben: [[T-0011 matches_any() Pfadabgleich reparieren]] (Ready)

## 2026-08-19 — T-0009 Beispielprojekt abgeschlossen (Umsetzungspfad Schritt 5)

- Ergebnis: `examples/slugify/` — zwei per TDD entwickelte, per `@contract` verlinkte
  Dateien; echte Vertragsänderung (`max_length`-Pflichtparameter) bricht den Consumer,
  `/contract-sync` findet und behebt es nachweislich (Blast Radius per `grep` verifiziert,
  keine undokumentierte Aufrufstelle). Volle Suite grün (24/24), CI-Check aus T-0010
  gegen die neuen Dateien gegenprobiert.
- Entscheidung mit dem Nutzer: `examples/`-Ordner statt eigener Branch (bleibt im PR
  sichtbar); Löschhinweis für echte Projekte in `examples/README.md`.
- Entscheidungen: keine neue ADR — Ordner-vs-Branch-Frage war Implementierungsdetail,
  nicht architekturrelevant genug für ein ADR
- Neu gelernt: nichts, was eine eigene Notiz rechtfertigt — bestätigt nur, dass
  `/contract-sync` wie in `.claude/rules/contracts.md` beschrieben funktioniert
- Offene Folgeaufgaben: keine

## 2026-08-19 — Abschließende Synchronisation (Umsetzungspfad Schritt 6)

- `/board-sync`: keine Abweichungen — alle elf Tasks haben genau eine Karte in der zur
  `status`-Frontmatter passenden Lane, keine Waisenkarten, Board-Format intakt
- `tools/check_vault.py`: 0 Fehler, 0 Warnungen
- `README.md` um `.github/workflows/`, `tests/` und `examples/` in der Struktur-Tabelle
  ergänzt, Durchsetzungs-Zeile um den CI-Check erweitert
- Damit ist der in dieser Sitzung geplante Umsetzungspfad vollständig abgearbeitet:
  T-0008 und T-0010 done, T-0009 done, T-0007 in Review (nur noch Obsidian-Rendering
  durch den Menschen offen), T-0011 als dokumentierte Folgeaufgabe in Ready
- Offene Folgeaufgaben: [[T-0011 matches_any() Pfadabgleich reparieren]] (Ready),
  Obsidian-Rendering für T-0007 (Mensch)

## 2026-08-19 — Kanban-Plugin nachrecherchiert, Doku korrigiert (zu T-0007)

- Anlass: Nutzerfrage, welches Obsidian-Plugin nötig ist und ob das Kanban die Tests
  verkompliziert
- Ergebnis: **nein, kein Code hängt am Plugin** — `session_brief.py` parst das Board mit
  eigener Regex, die 24 Tests brauchen weder Obsidian noch Plugin, `.obsidian/` aktiviert
  nur Core-Plugins. `board.md` funktioniert mit Plugin als Drag-and-Drop-Board und ohne als
  klickbare Checkliste.
- Dabei Doku-Fehler gefunden: `README.md` und [[Obsidian-Kanban Dateiformat]] nannten
  `obsidian-community/obsidian-kanban`; kanonisch ist `community-archive/obsidian-kanban`.
  Wartungslage war untertrieben (letzter Push 2026-03-06, 599 offene Issues, Org heißt
  `community-archive`). Alles korrigiert, inklusive Manifest-Eintrag.
- Entscheidung des Nutzers: Plugin-Format behalten, nur Doku ehrlich machen. Als
  Nachprüfung in [[ADR-0003 Kanban-Board mit Wikilink-Karten]] festgehalten — Entscheidung
  bleibt bestehen, Revidierungsbedingungen sind nicht erfüllt.
- Beobachten: `xiwcx/obsidian-bases-kanban` würde Lanes direkt aus dem `status:`-Feld
  bilden und `board.md` überflüssig machen. Bewusst nicht umgestellt.
- Offene Folgeaufgaben: keine

## 2026-08-19 — T-0007 Selbstverifikation abgeschlossen

- Letztes offenes Kriterium vom Nutzer bestätigt: Board rendert mit installiertem
  Kanban-Plugin korrekt in Obsidian. Damit alle sieben Akzeptanzkriterien erfüllt.
- Gesamtbilanz der Selbstverifikation: drei echte Defekte gefunden, die reines Codelesen
  nicht gezeigt hätte — Regex-Bug in `session_brief.py` (behoben), Glob-Bug in
  `_common.py#matches_any()` ([[T-0011 matches_any() Pfadabgleich reparieren]]), falsche
  Plugin-Adresse in README und Wissensnotiz (korrigiert).
- Offene Folgeaufgaben: [[T-0011 matches_any() Pfadabgleich reparieren]] (Ready)

## 2026-08-19 — T-0011 matches_any() repariert

- Ergebnis: `fnmatch` in `.claude/hooks/_common.py` durch eigene, pfadbewusste
  Glob-Übersetzung ersetzt (`glob_regex()` + `next_token()`). `/` ist jetzt echte
  Segmentgrenze, `*` läuft nicht mehr darüber hinweg.
- TDD sauber durchlaufen: 12 Fälle in `tests/hooks/test_common.py` zuerst, davon 2 rot
  (der dokumentierte Bug), danach alle grün. Volle Suite 36/36, `ruff` sauber.
- Der eigene Quality-Hook hat den ersten Fix-Entwurf zu Recht beanstandet
  (`elif`-Kette, Tiefe 5 > 3) → auf eine Token-Tabelle umgebaut. Die Regel greift real,
  auch gegen den Agenten, der sie gerade anwendet.
- Zwei gewollte Nebeneffekte: Pfade außerhalb des Projekts gelten nicht mehr als exempt
  (fail closed), und `generated_reports/` wird nicht mehr fälschlich von `**/generated/**`
  verschluckt.
- Neu gelernt: [[matches_any() exemption trifft falsche Pfade]] um Lösung, Regex-Tabelle
  und eine Sackgasse ergänzt (End-to-End-Prüfung ohne mitkopierte `config.json` lässt
  alles blocken und sieht nach kaputtem Fix aus).
- Offene Folgeaufgaben: keine

## 2026-08-19 — Anforderungsteil geplant und T-0012 umgesetzt

- Anlass: Vorgabe des Nutzers, dass das Template eine vereinfachte, Vibe-Coding-taugliche
  Fassung von Sommervilles *Software Engineering* durchführen soll — mit verpflichtender
  Anforderungserhebung vor dem Codieren
- Analyse: das Template beginnt heute beim Task. Sommerville Kapitel 4 fehlt vollständig;
  Akzeptanzkriterien sind ein Ersatz für Anforderungen und sagen nie, *warum* es das System
  geben soll
- Entscheidung [[ADR-0005 Anforderungen als Pflicht vor dem Code]], Recherche in
  [[Sommerville Software Engineering - was das Template übernimmt]], sechs Tasks
  (T-0012 bis T-0017) angelegt
- **Wichtige Korrektur durch den Nutzer während der Umsetzung:** der erste Entwurf sah rein
  inkrementelle Erhebung vor. Das beschreibt das Zielartefakt zu unsicher. Jetzt zwei
  Ebenen — Rahmen vorab vollständig, Details je Entwicklungsstufe mit Nachschärfen beim
  Codieren — plus Startgate in `baseline.md`. ADR vor dem Bauen überarbeitet.
- T-0012 fertig: `.claude/rules/requirements.md`, `knowledge/05-requirements/` mit sechs
  Rahmendokumenten, Anforderungsvorlage, Konventionen und `CLAUDE.md` angepasst
  (116 Zeilen, Grenze 200), `check_vault.py` um `requirement` erweitert
- Volle Suite 38/38, `ruff` sauber, Vault 0 Fehler
- Offene Folgeaufgaben: [[T-0013 Skills für die Anforderungserhebung]] und die weiteren
  aus dem Anforderungspaket (Ready)

## 2026-08-19 — T-0013 Skills für die Anforderungserhebung

- Ergebnis: `/req-elicit`, `/req-validate`, `/req-change` unter `.claude/skills/req-*/`.
  Damit sind die Regeln aus T-0012 erstmals benutzbar statt nur beschrieben.
- `/req-elicit` schaltet über `baseline_status` zwischen Rahmen- und Stufenmodus. Zwei
  Regeln hält er hart durch: das Gate nie selbst öffnen, und unprüfbare Formulierungen
  nicht entgegennehmen, sondern messbar umformuliert zurückgeben.
- `/req-change` folgt der Struktur von `/contract-sync`, damit beide gleich zu lesen sind;
  zusätzlich die Unterscheidung Präzisierung vs. echte Änderung.
- Trockenlauf bestanden: Anforderung nach Vorlage → `check_vault.py` 0 Fehler.
  Gegenprobe mit fehlerhafter Anforderung → 2 Fehler, Exit 1. Die Prüfung greift.
- Entscheidungen: keine neuen ADR nötig — alles folgt aus
  [[ADR-0005 Anforderungen als Pflicht vor dem Code]]
- Neu gelernt: nichts, was eine eigene Notiz rechtfertigt
- Offene Folgeaufgaben: [[T-0014 Rückverfolgbarkeit maschinell prüfen]] (Ready)

## 2026-08-19 — T-0014 Rückverfolgbarkeit maschinell geprüft

- Ergebnis: `tools/check_traceability.py` (in der CI) und `.claude/hooks/check_task.py`
  (blockierend beim Schreiben). Damit ist die Anforderungspflicht durchgesetzt statt
  nur beschrieben. 21 neue Tests, Suite 59/59.
- Geprüft wird in beide Richtungen: Task ohne Anforderung, unbekannte Anforderung,
  einseitiger Verweis, `umgesetzt` mit offenen Tasks (Fehler); vereinbarte Anforderung
  ohne Task (Warnung).
- **Alle 17 bestehenden Tasks verletzten die Regel sofort.** Rückwirkend als
  `infrastruktur:` markiert, damit die neue CI nicht rot startet — mit zwei ehrlich
  unterschiedenen Begründungen. Vorläufig, [[T-0017 Anforderungskette am Beispiel nachweisen]]
  ersetzt sie durch echte Bezüge.
- Zwei eigene Fehler unterwegs: Markierungsskript prüfte zuerst den ganzen Dateitext statt
  nur das Frontmatter und übersprang drei Tasks; der Quality-Hook monierte
  `check_backlinks()` mit Tiefe 4 → `link_mismatch()` ausgelagert.
- Bekannte Lücke dokumentiert: der Hook greift nur bei Write/Edit, nicht bei Bash-Schreiben.
  Die CI-Prüfung deckt das ab, aber erst beim PR.
- Entscheidungen: keine neuen ADR nötig
- Neu gelernt: nichts, was eine eigene Notiz rechtfertigt
- Offene Folgeaufgaben: [[T-0015 Bestehenden Arbeitszyklus anpassen]] (Ready)

## 2026-08-19 — T-0015 Arbeitszyklus an Anforderungen angepasst

- Ergebnis: `/task-new`, `/task-next`, `/task-done`, `/bootstrap`, `workflow.md` und
  `session_brief.py` kennen jetzt Anforderungen. Suite 62/62.
- Die eigentliche Lücke war `/bootstrap`: es seedete direkt das Board. Jetzt kommt ein
  Schritt „Rahmen erheben" davor — ein Board vor den Anforderungen lädt genau zu dem ein,
  was die Regel verhindern soll.
- `/task-next` prüft in Schritt 0 das Gate und in Schritt 2, dass die verlinkte Anforderung
  `vereinbart` ist. Auf einem Entwurf zu bauen ist der kurze Weg zur Nacharbeit.
- **Eigene Regel gerissen und repariert:** `requirements.md` brachte die immer geladenen
  Regeln auf 335 Zeilen; ADR-0001 begrenzt sie auf 300. Auf 88 Zeilen gekürzt, Summe jetzt
  exakt 300. Die Details der fünf Validierungsprüfungen stehen ohnehin in `/req-validate` —
  sie doppelt zu führen war der Fehler.
- Entscheidungen: keine neuen ADR nötig
- Neu gelernt: nichts, was eine eigene Notiz rechtfertigt
- Offene Folgeaufgaben: [[T-0016 Architekturschritt aus Qualitätsanforderungen]] (Ready)
